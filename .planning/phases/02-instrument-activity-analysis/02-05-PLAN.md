---
phase: 02-instrument-activity-analysis
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - src/components/InstrumentRoleOverlay.tsx
  - src/App.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "A React component reads useAppStore().instrumentRoles and renders per-instrument role labels visible to the user"
    - "Each instrument displays its current role label (soloing/comping/holding/silent) updating in real time during playback"
    - "Each instrument displays its current activity score (0.0–1.0) as a visual bar or numeric value"
    - "The overlay is positioned near the canvas so the user sees it in context with the visualizer"
    - "The component is minimal and disposable — Phase 5 (Canvas Node Graph) will replace it entirely"
  artifacts:
    - path: "src/components/InstrumentRoleOverlay.tsx"
      provides: "Lightweight React component displaying per-instrument role labels and activity scores from Zustand and audioStateRef"
    - path: "src/App.tsx"
      provides: "InstrumentRoleOverlay rendered below the VisualizerCanvas when file is loaded"
  key_links:
    - from: "src/components/InstrumentRoleOverlay.tsx"
      to: "src/store/useAppStore.ts"
      via: "useAppStore(s => s.instrumentRoles)"
      pattern: "instrumentRoles"
    - from: "src/App.tsx"
      to: "src/components/InstrumentRoleOverlay.tsx"
      via: "JSX render inside isFileLoaded block"
      pattern: "InstrumentRoleOverlay"
---

<objective>
Create a minimal React overlay component that reads the Zustand `instrumentRoles` map and displays per-instrument role labels and activity scores to the user. This closes the Phase 2 verification gap: "Role labels update at ~10fps and are recognizably correct on a real jazz recording — visible to the user."

Purpose: The computation pipeline is complete (INST-01 through INST-09 all satisfied). The only gap is that no UI surface reads `instrumentRoles` from Zustand. This plan adds a lightweight disposable component — Phase 5 will replace the canvas with proper instrument nodes, and Phase 7 adds full React panels. This component exists solely to make Phase 2 analysis data visible during playback.

Output: InstrumentRoleOverlay.tsx component, wired into App.tsx below the canvas.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-instrument-activity-analysis/02-VERIFICATION.md
@src/store/useAppStore.ts
@src/audio/types.ts
@src/App.tsx
@src/components/VisualizerCanvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InstrumentRoleOverlay component</name>
  <files>
    src/components/InstrumentRoleOverlay.tsx
  </files>
  <action>
    Create src/components/InstrumentRoleOverlay.tsx — a minimal React component that displays the four instrument role labels and activity scores.

    **Component: InstrumentRoleOverlay**

    Props:
    - audioStateRef: MutableRefObject&lt;AudioStateRef&gt; — needed to read activity scores from audioStateRef.current.analysis.instruments (activity scores live on the ref, NOT in Zustand)

    State/hooks:
    - `const instrumentRoles = useAppStore(s => s.instrumentRoles);` — reads role labels from Zustand (triggers re-render on role change)
    - `const [activityScores, setActivityScores] = useState<Record<string, number>>({});` — local state for activity score display
    - A useEffect with a 100ms setInterval that reads `audioStateRef.current.analysis?.instruments` and copies each instrument's `activityScore` into local state. Clean up interval on unmount. This gives ~10fps UI updates for activity scores without wiring them through Zustand (scores change every tick, roles only change occasionally).

    Render:
    - A horizontal flex row (gap-4) of 4 instrument cards
    - Each card is a small rounded div (bg #13131f, border rgba(99,102,241,0.3), padding 12px 16px)
    - Card contents (top to bottom):
      1. Instrument name — capitalize first letter, white text, font-medium, text-sm
      2. Role label badge — colored background pill:
         - soloing: bg #f59e0b (amber), text black
         - comping: bg #3b82f6 (blue), text white
         - holding: bg #6b7280 (gray), text white
         - silent: bg #1f2937 (dark gray), text #6b7280
         - If role not yet assigned (empty string or missing): show "---" in gray
      3. Activity bar — a small horizontal bar (height 4px, width 100%) with a filled portion:
         - Background: rgba(255,255,255,0.1)
         - Fill: same color as the role badge, width = activityScore * 100%
         - Rounded ends (border-radius 2px)
      4. Activity score numeric — text-xs, text gray-400, right-aligned: e.g. "0.72"

    Instrument order: bass, drums, keyboard, guitar (matches the hardcoded lineup)

    The entire component should be wrapped in a comment: {/* Phase 2 gap closure — replaced by Phase 5 Canvas Node Graph */}

    Style using inline styles and Tailwind classes consistent with the rest of the app. No new dependencies.

    File header comment:
    ```
    /**
     * InstrumentRoleOverlay.tsx — Minimal Phase 2 gap closure component.
     *
     * Displays per-instrument role labels and activity scores read from
     * Zustand (roles) and audioStateRef (activity scores).
     *
     * TEMPORARY: This component will be replaced entirely by Phase 5
     * (Canvas Node Graph) which renders instrument nodes with role-based
     * visuals directly on the canvas.
     */
    ```
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors.
    Verify the component imports useAppStore and reads instrumentRoles.
    Verify the component reads audioStateRef.current.analysis.instruments for activity scores.
    Verify the 100ms interval is cleaned up on unmount.
    Verify all four role labels have distinct visual styling.
  </verify>
  <done>
    InstrumentRoleOverlay component created — reads Zustand instrumentRoles for role labels and audioStateRef for activity scores at ~10fps. Minimal disposable component for Phase 2 gap closure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire InstrumentRoleOverlay into App.tsx</name>
  <files>
    src/App.tsx
  </files>
  <action>
    Modify src/App.tsx to render InstrumentRoleOverlay below the VisualizerCanvas.

    1. Add import at top:
       ```
       import { InstrumentRoleOverlay } from './components/InstrumentRoleOverlay';
       ```

    2. Inside the `{isFileLoaded && (...)}` block that contains `<VisualizerCanvas>` (around line 72), add InstrumentRoleOverlay immediately after VisualizerCanvas, inside the same wrapper div:
       ```
       {isFileLoaded && (
         <div className="w-full max-w-4xl">
           <VisualizerCanvas audioStateRef={audioStateRef} />
           {/* Phase 2 gap closure — replaced by Phase 5 Canvas Node Graph */}
           {!isCalibrating && (
             <InstrumentRoleOverlay audioStateRef={audioStateRef} />
           )}
         </div>
       )}
       ```
       The `!isCalibrating` guard ensures the overlay only shows after calibration completes (when analysis state is initialized and roles start updating). Before calibration, there are no roles to display.

    3. Do NOT change any other part of App.tsx.
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero errors.
    Run `npm run dev`, upload a jazz audio file, wait for calibration.
    After calibration completes, verify:
    - Four instrument cards appear below the canvas (bass, drums, keyboard, guitar)
    - Role labels update during playback (should see soloing/comping/holding/silent changes)
    - Activity bars move in response to musical content
    - Activity scores update numerically
    Verify the overlay does NOT appear during calibration.
    Run `npx tsc --noEmit` again — zero errors.
  </verify>
  <done>
    InstrumentRoleOverlay wired into App.tsx below the canvas. Role labels and activity scores are now visible to the user during playback — Phase 2 verification gap closed.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` returns zero errors
2. InstrumentRoleOverlay reads `useAppStore(s => s.instrumentRoles)` — Zustand data is consumed by a React component
3. Activity scores read from audioStateRef.current.analysis.instruments at ~10fps via setInterval
4. Four instrument cards render with role label badges and activity bars
5. Role labels visually distinguish soloing (amber), comping (blue), holding (gray), silent (dark)
6. Overlay appears only after calibration completes (guarded by !isCalibrating)
7. Upload audio file → play → role labels update visibly during playback
8. Component is clearly marked as temporary (Phase 5 replacement)
</verification>

<success_criteria>
- Role labels (soloing/comping/holding/silent) are visible to the user per instrument during playback
- Activity scores (0.0–1.0) are visible per instrument with a visual bar
- Zustand instrumentRoles map is no longer orphaned — a React component consumes it
- Phase 2 Truth 2 is satisfied: "Role labels update at ~10fps and are recognizably correct on a real jazz recording — visible to the user"
- Minimal footprint — one new component file, one small App.tsx edit
</success_criteria>

<output>
After completion, create `.planning/phases/02-instrument-activity-analysis/02-05-SUMMARY.md`
</output>
