---
phase: 09-data-layer-and-structural-refactor
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/audio/InstrumentActivityScorer.ts
  - src/components/BandSetupPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Calibration pass adapts to whichever 2-8 instruments are selected without code changes"
    - "Role classification (soloing/comping/holding/silent) works for all 8 instrument types"
    - "initAnalysisState creates InstrumentAnalysis entries only for the instruments in the active lineup"
    - "BandSetupPanel shows all 8 instruments as selectable options with icons and frequency labels"
  artifacts:
    - path: "src/audio/InstrumentActivityScorer.ts"
      provides: "initAnalysisState that works for any InstrumentName[] lineup"
      contains: "MID_RANGE_INSTRUMENTS"
    - path: "src/components/BandSetupPanel.tsx"
      provides: "8-instrument selection UI with icons and frequency labels"
      contains: "saxophone"
  key_links:
    - from: "src/components/BandSetupPanel.tsx"
      to: "src/store/useAppStore.ts"
      via: "setLineup Zustand action"
      pattern: "setLineup"
---

<objective>
Verify the scorer/calibration generalize to variable lineups and expand the BandSetupPanel UI to show all 8 instruments.

Purpose: Plan 01 expanded the type foundation. This plan confirms that initAnalysisState, computeActivityScore, classifyRole, and calibration all work unchanged for 2-8 instruments (they already iterate generically). The BandSetupPanel UI must offer all 8 instruments so users can build any combo.
Output: Verified scorer generalization + updated BandSetupPanel with 8 instruments.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/09-data-layer-and-structural-refactor/09-01-SUMMARY.md

@src/audio/InstrumentActivityScorer.ts
@src/audio/CalibrationPass.ts
@src/components/BandSetupPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify scorer and calibration generalization (read-only audit + minimal fixes)</name>
  <files>src/audio/InstrumentActivityScorer.ts</files>
  <action>
Read and audit these functions to confirm they work for any InstrumentName[] lineup of 2-8 instruments:

1. **initAnalysisState(lineup, fftSize)** — Creates InstrumentAnalysis[] by iterating lineup. Already generic. Confirm it calls resolveBandsForInstrument for each instrument. No code change expected.

2. **computeActivityScore(...)** — Takes InstrumentAnalysis + bands + calibration. Already generic — operates on one instrument at a time. No code change expected.

3. **classifyRole(...)** — Assigns soloing/comping/holding/silent based on activity score. Already generic. No code change expected.

4. **CalibrationPass.ts runCalibrationPass(...)** — Iterates audioStateRef.analysis.instruments[]. Already generic — calibrates whatever instruments are in the array. Confirm it uses resolveBandsForInstrument. No code change expected.

If any function has a hardcoded assumption about 4 instruments or specific instrument names, fix it. The expectation is zero changes needed — the existing code was already designed to iterate generically.

Document findings: which functions are confirmed generic and which (if any) needed fixes.
  </action>
  <verify>Read each function and confirm it iterates the instruments array without hardcoded names. Run `npx tsc --noEmit 2>&1 | grep "InstrumentActivityScorer\|CalibrationPass"` — should show no errors in these files.</verify>
  <done>All scorer and calibration functions confirmed to work for any 2-8 instrument lineup without modification, OR any necessary fixes applied.</done>
</task>

<task type="auto">
  <name>Task 2: Expand BandSetupPanel to 8 instruments</name>
  <files>src/components/BandSetupPanel.tsx</files>
  <action>
1. Expand AVAILABLE_INSTRUMENTS to include all 8:
```typescript
const AVAILABLE_INSTRUMENTS = ['keyboard', 'bass', 'drums', 'guitar', 'saxophone', 'trumpet', 'trombone', 'vibes'] as const;
```

2. Add entries to INSTRUMENT_ICONS:
```typescript
saxophone: '\u{1F3B7}',  // saxophone emoji
trumpet: '\u{1F3BA}',    // trumpet emoji
trombone: '\u{1F3B6}',   // musical notes (no trombone emoji exists)
vibes: '\u{1F3B5}',      // musical note (no vibraphone emoji exists)
```
Note: If the existing icons use different emoji, match the style. The key is that each instrument has a distinct visual icon.

3. Add entries to BAND_LABELS (frequency ranges from ARCHITECTURE.md):
```typescript
saxophone: '250-2000 Hz',
trumpet: '300-3000 Hz',
trombone: '250-2000 Hz',
vibes: '250-3000 Hz',
```

4. Do NOT change the component logic (handleAdd, handleRemove, the locked state behavior). The existing code already works generically with any AVAILABLE_INSTRUMENTS array — it filters, maps, and renders from the constant.

5. Do NOT add family grouping or count badge yet — that is Phase 10 (BAND-01, BAND-02) scope. This plan only adds the 4 new instruments as selectable options.
  </action>
  <verify>Run `npx tsc --noEmit 2>&1 | grep "BandSetupPanel"` — no errors. Run `npx vite build` to confirm the app builds. Visually: the 4 new instruments should appear in the setup panel dropdown.</verify>
  <done>BandSetupPanel shows all 8 instruments. Each has an icon and frequency range label. Existing add/remove/lock behavior unchanged.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit 2>&1 | grep "InstrumentActivityScorer\|CalibrationPass\|BandSetupPanel"` — zero errors in these files.
2. BandSetupPanel renders all 8 instruments in the available instruments list.
3. Selecting saxophone, trumpet, trombone, or vibes in the lineup does not crash the app (though canvas will still be broken until Plans 03-04 are complete).
</verification>

<success_criteria>
- initAnalysisState, computeActivityScore, classifyRole, runCalibrationPass all confirmed generic for 2-8 instruments
- BandSetupPanel shows 8 instruments with icons and frequency labels
- No TypeScript errors in modified files
- App builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-layer-and-structural-refactor/09-02-SUMMARY.md`
</output>
