---
phase: 14-tech-debt-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/canvas/edges/drawCommunicationEdges.ts
  - src/components/VisualizerCanvas.tsx
autonomous: true

must_haves:
  truths:
    - "All edge types resolve correctly from EDGE_TYPE without any fallback operator for every valid lineup"
    - "Malformed pair keys produce a console.warn and skip rendering instead of crashing the canvas"
    - "Switching between recordings with different lineups hot-swaps the canvas without requiring a full remount"
  artifacts:
    - path: "src/canvas/edges/drawCommunicationEdges.ts"
      provides: "Key validation guard and no fallback operator"
      contains: "console.warn"
    - path: "src/components/VisualizerCanvas.tsx"
      provides: "Reactive lineup from Zustand selector"
      contains: "useAppStore(s => s.lineup)"
  key_links:
    - from: "src/canvas/edges/drawCommunicationEdges.ts"
      to: "src/canvas/edges/edgeTypes.ts"
      via: "EDGE_TYPE lookup with pre-validated key"
      pattern: "EDGE_TYPE\\[key\\]"
    - from: "src/components/VisualizerCanvas.tsx"
      to: "src/store/useAppStore.ts"
      via: "reactive Zustand selector for lineup"
      pattern: "useAppStore\\(s\\s*=>\\s*s\\.lineup\\)"
---

<objective>
Close three tech debt items from the v1.1 audit: remove the dead `?? 'support'` fallback in drawCommunicationEdges.ts (DEBT-01), add a crash guard for malformed pair keys in the same file (DEBT-02), and make VisualizerCanvas read lineup reactively from Zustand instead of a one-time getState() call (DEBT-03).

Purpose: Eliminate dead code, prevent potential canvas crashes from bad data, and fix a brittleness bug where hot-swapping lineups requires a full remount.
Output: Two cleaned-up source files with no functional behavior change for valid inputs, plus resilience for invalid inputs and reactive lineup updates.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/canvas/edges/drawCommunicationEdges.ts
@src/canvas/edges/edgeTypes.ts
@src/components/VisualizerCanvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pair key guard and remove fallback (DEBT-01 + DEBT-02)</name>
  <files>src/canvas/edges/drawCommunicationEdges.ts</files>
  <action>
In drawCommunicationEdges.ts, apply DEBT-01 and DEBT-02 atomically:

1. Add a module-level constant for key validation:
   ```typescript
   const KEY_PATTERN = /^[a-z]+_[a-z]+$/;
   ```
   Place it near the top of the file after the imports, before the VisualState type.

2. In the Pass 1 loop (line ~137, inside `for (const [idxA, idxB, key] of pairs)`), REPLACE the current line 214:
   ```typescript
   const edgeType: EdgeType = EDGE_TYPE[key] ?? 'support';
   ```
   WITH a guard-then-assign pattern:
   ```typescript
   if (!KEY_PATTERN.test(key) || !(key in EDGE_TYPE)) {
     console.warn(`[drawCommunicationEdges] Skipping invalid pair key: ${JSON.stringify(key)}`);
     continue;
   }
   const edgeType: EdgeType = EDGE_TYPE[key];
   ```

This must be done as a single atomic change -- the guard MUST exist before the fallback is removed, so they go in the same commit. The guard validates both the format (lowercase_lowercase) and the presence in the EDGE_TYPE lookup table.

Do NOT change anything else in this file. The guard replaces one line with four lines. All other code remains identical.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors. Grep the file to confirm:
- `?? 'support'` does NOT appear in drawCommunicationEdges.ts
- `KEY_PATTERN` is defined
- `console.warn` with `Skipping invalid pair key` exists
- `continue` follows the warn
  </verify>
  <done>The `?? 'support'` fallback is removed. Invalid pair keys log a warning and skip rendering. Valid pair keys resolve from EDGE_TYPE without fallback. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Make lineup reactive in VisualizerCanvas (DEBT-03)</name>
  <files>src/components/VisualizerCanvas.tsx</files>
  <action>
In VisualizerCanvas.tsx, convert the one-time lineup read to a reactive Zustand selector:

1. At the component level (inside the function body, before the useEffect), add a reactive selector:
   ```typescript
   const lineup = useAppStore(s => s.lineup);
   ```

2. Inside the useEffect, REMOVE the line:
   ```typescript
   const lineup = useAppStore.getState().lineup;
   ```
   The `lineup` variable is now available from the component scope above.

3. Add `lineup` to the useEffect dependency array. Change:
   ```typescript
   }, [audioStateRef]);
   ```
   to:
   ```typescript
   }, [audioStateRef, lineup]);
   ```

The existing cleanup function already calls `renderer.destroy()` and disconnects the observer, so when `lineup` changes, the effect will properly tear down the old renderer and create a new one with the updated lineup. No other changes needed.

Do NOT change the onCanvasReady callback, the click handler, or any of the renderer.setOn* wiring. Those all remain identical inside the effect.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors. Grep the file to confirm:
- `useAppStore.getState().lineup` does NOT appear
- `useAppStore(s => s.lineup)` appears at component level
- `lineup` is in the useEffect dependency array
  </verify>
  <done>Lineup is read reactively from Zustand. When the user switches recordings with different lineups, the useEffect re-runs, destroys the old renderer, and creates a new one with the correct lineup -- no full remount needed.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -n "?? 'support'" src/canvas/edges/drawCommunicationEdges.ts` returns nothing
3. `grep -n "KEY_PATTERN" src/canvas/edges/drawCommunicationEdges.ts` returns the constant and usage
4. `grep -n "getState().lineup" src/components/VisualizerCanvas.tsx` returns nothing
5. `grep -n "useAppStore(s => s.lineup)" src/components/VisualizerCanvas.tsx` returns the selector line
6. `grep -n "audioStateRef, lineup" src/components/VisualizerCanvas.tsx` confirms dependency array
</verification>

<success_criteria>
- DEBT-01: The `?? 'support'` fallback is gone from drawCommunicationEdges.ts
- DEBT-02: Malformed pair keys produce a console.warn and `continue` (no crash)
- DEBT-03: VisualizerCanvas reads lineup reactively; lineup is in useEffect deps
- TypeScript compiles cleanly with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-tech-debt-and-polish/14-01-SUMMARY.md`
</output>
