---
phase: 08-advanced-features
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/components/ConversationLogPanel.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "ConversationLogPanel renders as an expandable drawer below ChordLogPanel"
    - "Each call-response entry shows timestamp, 'Keyboard calls' → 'Guitar responds', and gap duration"
    - "Clicking an entry seeks playback to the call timestamp"
    - "The collapsed header shows the total count of call-response events detected"
    - "Real-time entries appear as call-response events are detected during playback"
  artifacts:
    - path: "src/components/ConversationLogPanel.tsx"
      provides: "Expandable drawer showing timestamped call-response events"
      exports: ["ConversationLogPanel"]
    - path: "src/App.tsx"
      provides: "ConversationLogPanel integrated below ChordLogPanel"
      contains: "ConversationLogPanel"
  key_links:
    - from: "src/components/ConversationLogPanel.tsx"
      to: "src/store/useAppStore.ts"
      via: "reads callResponseLog from Zustand"
      pattern: "useAppStore.*callResponseLog"
    - from: "src/components/ConversationLogPanel.tsx"
      to: "src/hooks/useSeek.ts"
      via: "click-to-seek on entry timestamp"
      pattern: "useSeek"
---

<objective>
Create ConversationLogPanel component showing timestamped call-and-response events (MEL-05).

Purpose: Musicians need to see WHEN call-and-response exchanges happen. This panel mirrors ChordLogPanel's expandable drawer pattern — timestamped entries, color-coded, clickable to seek. It makes the detected conversations browsable.

Output: ConversationLogPanel.tsx created; App.tsx updated to render it below ChordLogPanel.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-advanced-features/08-RESEARCH.md
@.planning/phases/08-advanced-features/08-02-SUMMARY.md

@src/components/ChordLogPanel.tsx
@src/store/useAppStore.ts
@src/hooks/useSeek.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConversationLogPanel component</name>
  <files>
    src/components/ConversationLogPanel.tsx
  </files>
  <action>
Create ConversationLogPanel.tsx following the ChordLogPanel.tsx pattern exactly.

**Structure:**
```typescript
import { useState } from 'react';
import type { MutableRefObject } from 'react';
import type { AudioStateRef, CallResponseEntry } from '../audio/types';
import { useSeek } from '../hooks/useSeek';
import { useAppStore } from '../store/useAppStore';

interface ConversationLogPanelProps {
  audioStateRef: MutableRefObject<AudioStateRef>;
}
```

**Formatting helper:**
```typescript
function formatTimestamp(sec: number): string {
  const m = Math.floor(sec / 60);
  const s = sec % 60;
  return `${m}:${s.toFixed(1).padStart(4, '0')}`;
}
```

**Component behavior:**
1. Expandable drawer with toggle (collapsed by default).
2. Header shows "Conversations" label and count badge: `{callResponseLog.length} exchanges`.
3. When expanded, renders a scrollable list of entries in reverse chronological order (most recent first).
4. Each entry row shows:
   - Timestamp: `formatTimestamp(entry.callSec)` in muted text
   - Direction label: "KB calls → GT responds" in purple (#a855f7) text
   - Gap: `(${entry.gapSec.toFixed(1)}s gap)` in dim text
5. Clicking an entry calls `seekTo(entry.callSec)` to jump playback to the call moment.
6. Cursor pointer on each entry, hover highlight similar to ChordLogPanel rows.

**Styling:** Match ChordLogPanel exactly:
- Container: bg-[#13131f], border border-indigo-500/20, rounded
- Header: flex justify-between, cursor-pointer, p-3
- Expanded body: max-h-48 overflow-y-auto, divide-y divide-indigo-500/10
- Entry rows: px-3 py-2, hover:bg-indigo-500/10, cursor-pointer
- Purple accent (#a855f7) for the call-response text
- Empty state: "No call-response exchanges detected yet" in dim text

**Read callResponseLog from Zustand directly (not via interval polling — log entries are discrete events, not continuous data).**
  </action>
  <verify>
`npx tsc --noEmit` — no type errors. Component renders without crash in isolation.
  </verify>
  <done>
ConversationLogPanel.tsx created with expandable drawer, timestamped call-response entries, click-to-seek, and count badge in header. Matches ChordLogPanel visual pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate ConversationLogPanel into App.tsx</name>
  <files>
    src/App.tsx
  </files>
  <action>
1. Import ConversationLogPanel from `./components/ConversationLogPanel`.

2. Render `<ConversationLogPanel audioStateRef={audioStateRef} />` immediately below the ChordLogPanel in the JSX tree. Both panels should be inside the same container/wrapper that holds the timeline and chord log.

3. Wire the onMelodyUpdate callback in CanvasRenderer (or wherever CanvasRenderer callbacks are set up). The callback should:
   - Call `useAppStore.getState().setMelodyState(kbMelodic, gtMelodic)` to update melodic flags.
   - If `callResponse` is not null, call `useAppStore.getState().addCallResponseEntry(callResponse)`.

   **Note:** Plan 08-02 already added the edge flash trigger (`edgeAnimStates['guitar_keyboard'].callResponseFlashIntensity = 1.0`) inside CanvasRenderer's onMelodyUpdate handler. This task only needs to add the Zustand bridge calls alongside it — do NOT duplicate or overwrite the flash trigger. If 08-02 placed the flash trigger inside onMelodyUpdate, add the Zustand calls in the same handler block.

   Find where onRoleChange, onChordChange, onTensionUpdate, and onBeatUpdate are wired (likely in VisualizerCanvas.tsx or App.tsx) and add the onMelodyUpdate callback in the same pattern.

4. The ConversationLogPanel only renders when `isFileLoaded` is true (same visibility condition as ChordLogPanel).
  </action>
  <verify>
1. `npx tsc --noEmit` — no type errors
2. `npm run dev` — load audio:
   - ConversationLogPanel appears below ChordLogPanel
   - It starts empty with "No call-response exchanges detected yet"
   - During playback, if call-response events fire, entries appear in the panel
   - Clicking an entry seeks to that timestamp
   - Expanding/collapsing the drawer works
  </verify>
  <done>
ConversationLogPanel integrated into App.tsx below ChordLogPanel. onMelodyUpdate callback wired to bridge pitch/call-response state from AnalysisTick to Zustand. Panel shows real-time call-response entries during playback.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. ConversationLogPanel renders as expandable drawer below ChordLogPanel
3. Entries appear in real-time as call-response events are detected
4. Click-to-seek works on entries
5. Count badge updates in header
6. Empty state shown when no events detected
</verification>

<success_criteria>
MEL-05 (conversation log panel) fully implemented. Timestamped call-response entries with clickable seek, expandable drawer matching ChordLogPanel pattern, real-time updates during playback.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-features/08-03-SUMMARY.md`
</output>
