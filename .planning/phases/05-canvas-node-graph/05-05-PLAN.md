---
phase: 05-canvas-node-graph
plan: 05
type: execute
wave: 4
depends_on: ["05-03", "05-04"]
files_modified:
  - src/canvas/CanvasRenderer.ts
autonomous: false

must_haves:
  truths:
    - "All nodes pulse +2px on drum beats and +4px on downbeats"
    - "Canvas background subtly shifts from #0a0a0f to #0d0d18 on each beat over 200ms"
    - "Canvas animation stays smooth at 60fps during simultaneous FFT analysis on desktop"
    - "No visible jank on iPhone hardware during playback"
  artifacts:
    - path: "src/canvas/CanvasRenderer.ts"
      provides: "All-node beat pulse, background breath, complete Phase 5 rendering pipeline"
      contains: "bgPulseProgress"
  key_links:
    - from: "src/canvas/CanvasRenderer.ts"
      to: "audioStateRef.current.beat.lastDrumOnsetSec"
      via: "all-node pulse trigger"
      pattern: "lastDrumOnsetSec"
    - from: "src/canvas/CanvasRenderer.ts"
      to: "bgPulseProgress"
      via: "background color interpolation driven by beat"
      pattern: "bgPulseProgress"
---

<objective>
Add beat-responsive animations to all nodes (pulse on beat/downbeat) and background breath effect, then verify iOS performance.

Purpose: VIZ-10 (all-node pulse), VIZ-11 (background breath) complete the beat-responsive canvas. iOS verification confirms Phase 5 success criteria #5.
Output: Fully animated node graph with beat-synchronized pulse and background breathing.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-canvas-node-graph/05-RESEARCH.md
@src/canvas/CanvasRenderer.ts
@src/canvas/nodes/NodeAnimState.ts
@src/audio/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all-node beat pulse and background breath</name>
  <files>src/canvas/CanvasRenderer.ts</files>
  <action>
Add two final beat-responsive effects to CanvasRenderer:

**1. All-Node Beat Pulse (VIZ-10):**
- Add instance variable `private beatPulse = 0` — shared across all nodes, decays each frame
- On new drum onset detected (use a SINGLE onset detection at the top of the render loop, before the per-node loop, checking `beat.lastDrumOnsetSec !== this.lastSeenGlobalDrumOnset`): set `this.beatPulse = 2` (px)
- On new downbeat detected (check `beat.lastDownbeatSec !== this.lastSeenGlobalDownbeat`): set `this.beatPulse = 4` (px, overrides beat pulse since downbeat is also a beat)
- Add instance variables: `private lastSeenGlobalDrumOnset = -1`, `private lastSeenGlobalDownbeat = -1`
- Each frame: `this.beatPulse = lerpExp(this.beatPulse, 0, 0.88, deltaMs)`. Snap to 0 below 0.3px.
- In the per-node radius computation, ADD `this.beatPulse` to the target radius alongside `animState.radiusNudge`:
  `animState.currentRadius = lerpExp(animState.currentRadius, animState.baseRadius + animState.radiusNudge + this.beatPulse, 0.15, deltaMs)`
- This makes ALL nodes (bass, drums, keyboard, guitar) pulse on beats — drums gets the most movement since it also has its own +6px nudge.

**2. Background Breath (VIZ-11):**
- The `bgPulseProgress` instance variable was declared in 05-01 (initialized to 0).
- On new drum onset (same detection as above): set `this.bgPulseProgress = 1.0`
- Each frame before fillRect: `this.bgPulseProgress = Math.max(0, this.bgPulseProgress - deltaMs / 200)` (200ms decay)
- Compute background color: interpolate RGB channels from `{r:0x0a, g:0x0a, b:0x0f}` (#0a0a0f) to `{r:0x0d, g:0x0d, b:0x18}` (#0d0d18) using `bgPulseProgress` as t
- Set `ctx.fillStyle = \`rgb(${r},${g},${b})\`` before `ctx.fillRect(0, 0, w, h)`
- When bgPulseProgress is 0, the background stays at #0a0a0f (base dark)

**3. Guard all beat reads:** If `state.beat` is null or `state.beat.bpm` is null, skip onset detection (no pulses or background breath during rubato or before calibration).

**4. Ensure draw order is correct for the full Phase 5 pipeline:**
   1. Background fill (with breath pulse)
   2. For each node: glow (behind) -> node circle -> ripples (on top) -> label
   3. Tension meter (rightmost, on top of everything)

Verify the render loop is clean — no redundant onset detection. The global onset detection at the top sets `beatPulse` and `bgPulseProgress`. The per-instrument onset detection in the drums/bass blocks handles instrument-specific effects (nudge, breathing, ripples).
  </action>
  <verify>
`npx tsc --noEmit` passes. `npm run dev` — play audio:
- All 4 nodes visibly pulse together on drum beats (+2px)
- Stronger pulse on downbeats (+4px)
- Background color subtly lightens for 200ms on each beat
- Drums node still has its own additional +6px nudge on top of the shared pulse
- Bass node still breathes independently
  </verify>
  <done>All nodes pulse +2px on beat, +4px on downbeat. Background shifts from #0a0a0f to #0d0d18 over 200ms on each beat. Draw order correct: bg -> glow -> circle -> ripples -> labels -> tension meter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 5 node graph: diamond layout with 4 instrument nodes, role-based sizing/coloring, bass breathing glow with pocket-score color shift, drums beat nudge with ripples, all-node beat pulse, background breath. All animations frame-rate independent via deltaMs.</what-built>
  <how-to-verify>
1. Open `npm run dev` at localhost:5555
2. Upload a jazz recording with clear bass and drums
3. Verify during playback:
   a. 4 nodes visible in diamond layout (guitar top, drums left, keyboard right, bass bottom)
   b. Nodes change size/color as roles change (soloing=large/amber, silent=small/dark)
   c. Bass node has amber breathing glow that pulses with the beat
   d. Bass glow flashes bright on bass onsets with expanding ring
   e. Drums node nudges on beats with white-blue ripple rings
   f. Downbeats produce double ripples on drums
   g. All nodes pulse together on beats
   h. Background subtly lightens on each beat
4. Test on iPhone (if available):
   a. Upload and play same track
   b. Animation should be smooth — no visible jank
   c. Test with Low Power Mode OFF
5. Switch browser tabs during playback, return — no animation jumps
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero type errors
- All 5 Phase 5 success criteria confirmed:
  1. Canvas displays 2-4 instrument nodes in correct layout
  2. Nodes animate on drum beats with nudge and ripple
  3. Bass node glows amber, shifts with pocket score
  4. Role-based states visually distinct
  5. 60fps smooth on desktop, no visible jank on iPhone
</verification>

<success_criteria>
- All-node beat pulse visible (VIZ-10)
- Background breath visible (VIZ-11)
- Full Phase 5 animation pipeline working end-to-end
- iOS performance verified by human on real device (or noted as untested)
</success_criteria>

<output>
After completion, create `.planning/phases/05-canvas-node-graph/05-05-SUMMARY.md`
</output>
