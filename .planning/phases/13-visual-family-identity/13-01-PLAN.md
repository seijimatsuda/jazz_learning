---
phase: 13-visual-family-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/instrumentFamilies.ts
  - src/canvas/nodes/drawNode.ts
  - src/canvas/CanvasRenderer.ts
autonomous: true

must_haves:
  truths:
    - "Each instrument node displays a colored ring stroke outside its fill circle, color indicating family (brass/woodwind/rhythm/keyboard/strings)"
    - "Fill color still reflects current role (soloing/comping/holding/silent) — ring does not obscure or replace it"
    - "Same-family instruments appear adjacent on the circular layout (e.g., trumpet and trombone next to each other)"
    - "Bass remains at center position (index 0) regardless of family sort"
  artifacts:
    - path: "src/audio/instrumentFamilies.ts"
      provides: "FAMILY_RING_COLOR constant mapping family strings to hex colors"
      contains: "FAMILY_RING_COLOR"
    - path: "src/canvas/nodes/drawNode.ts"
      provides: "Ring stroke rendering with ringColor optional parameter"
      contains: "ringColor"
    - path: "src/canvas/CanvasRenderer.ts"
      provides: "Family-sorted instrumentOrder and ringColor passed to drawNode"
      contains: "FAMILY_SORT_ORDER"
  key_links:
    - from: "src/canvas/CanvasRenderer.ts"
      to: "src/audio/instrumentFamilies.ts"
      via: "import INSTRUMENT_FAMILIES and FAMILY_RING_COLOR"
      pattern: "FAMILY_RING_COLOR\\[family\\]"
    - from: "src/canvas/CanvasRenderer.ts"
      to: "src/canvas/nodes/drawNode.ts"
      via: "drawNode call with ringColor argument"
      pattern: "drawNode\\(ctx.*ringColor\\)"
---

<objective>
Add instrument family visual identity to canvas nodes: colored ring strokes indicating family membership (VIS-01) and family-sorted circular layout clustering related instruments (VIS-02).

Purpose: Users can instantly identify which instrument family each node belongs to (brass=amber, woodwind=emerald, rhythm=orange, keyboard=indigo, strings=fuchsia) and see related instruments grouped adjacently on the ring layout.

Output: Modified drawNode with ring stroke, FAMILY_RING_COLOR constants, family-sorted instrumentOrder in CanvasRenderer constructor.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-visual-family-identity/13-RESEARCH.md

@src/audio/instrumentFamilies.ts
@src/canvas/nodes/drawNode.ts
@src/canvas/CanvasRenderer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Family ring stroke on nodes (VIS-01)</name>
  <files>src/audio/instrumentFamilies.ts, src/canvas/nodes/drawNode.ts, src/canvas/CanvasRenderer.ts</files>
  <action>
1. In `src/audio/instrumentFamilies.ts`, add a new exported constant `FAMILY_RING_COLOR` mapping family strings to hex color strings:
   - rhythm: '#f97316' (orange-500)
   - brass: '#fbbf24' (amber-400)
   - woodwind: '#34d399' (emerald-400)
   - keyboard: '#818cf8' (indigo-400)
   - strings: '#e879f9' (fuchsia-400)
   Do NOT modify the existing `INSTRUMENT_FAMILIES` map or any other exports — Phase 12 disambiguators depend on them.

2. In `src/canvas/nodes/drawNode.ts`, add an optional `ringColor?: string` parameter as the 7th argument to `drawNode`. After the existing fill circle code and BEFORE the label code, add a ring stroke block:
   - Guard: `if (ringColor) { ... }`
   - Use `ctx.save()` / `ctx.restore()` to isolate lineWidth changes (prevents bleeding into subsequent edge draws)
   - Draw arc at `radius + 1.5` (not `radius`) so the ring sits OUTSIDE the fill circle, not overlapping it
   - Set `ctx.strokeStyle = ringColor`, `ctx.lineWidth = 3`, then `ctx.stroke()`
   - The ring inherits any `ctx.globalAlpha` set by the caller (Phase 12 confidence indicator) — this is correct behavior

3. In `src/canvas/CanvasRenderer.ts`, in the node draw loop (the `for` loop starting around line 537):
   - Add import for `INSTRUMENT_FAMILIES` and `FAMILY_RING_COLOR` from `'../audio/instrumentFamilies'`
   - Before each `drawNode` call (there are 3: drums branch, bass branch, and default branch), compute:
     ```
     const family = INSTRUMENT_FAMILIES[instrument] ?? 'rhythm';
     const ringColor = FAMILY_RING_COLOR[family] ?? '#64748b';
     ```
   - Pass `ringColor` as the 7th argument to all three `drawNode` calls
   - Compute family/ringColor ONCE before the if/else branches (since it's the same for all branches), then pass to each drawNode call
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors. Visually: every instrument node should display a colored ring outside its fill circle. The ring color should be stable (not animating) and distinct per family.
  </verify>
  <done>
All 3 drawNode call sites in CanvasRenderer pass ringColor. Ring renders outside the fill circle at radius+1.5. ctx.save/restore isolates lineWidth. FAMILY_RING_COLOR exported from instrumentFamilies.ts. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Family-sorted circular layout (VIS-02)</name>
  <files>src/canvas/CanvasRenderer.ts</files>
  <action>
In `src/canvas/CanvasRenderer.ts` constructor, AFTER the existing bass-to-index-0 reorder (lines ~188-196) and BEFORE `computeNodePositions` (line ~199):

1. Add a `FAMILY_SORT_ORDER` constant (can be module-level or inside the constructor — module-level preferred for clarity):
   ```typescript
   const FAMILY_SORT_ORDER: Record<string, number> = {
     rhythm:   0,  // drums (bass is always center, not on ring)
     keyboard: 1,  // keyboard, vibes
     strings:  2,  // guitar — adjacent to keyboard for jazz chordal clustering
     woodwind: 3,  // saxophone
     brass:    4,  // trumpet, trombone
   };
   ```
   Note: strings at sort value 2 places guitar adjacent to keyboard (sort value 1), which makes musical sense in jazz context where guitar and keyboard share harmonic/chordal roles.

2. Sort the ring instruments (everything except bass at index 0) by family:
   ```typescript
   const ringInstruments = this.instrumentOrder.slice(1);
   ringInstruments.sort((a, b) => {
     const fa = INSTRUMENT_FAMILIES[a] ?? 'rhythm';
     const fb = INSTRUMENT_FAMILIES[b] ?? 'rhythm';
     return (FAMILY_SORT_ORDER[fa] ?? 99) - (FAMILY_SORT_ORDER[fb] ?? 99);
   });
   this.instrumentOrder = this.instrumentOrder[0] === 'bass'
     ? ['bass', ...ringInstruments]
     : [...ringInstruments];  // bass absent — all instruments on ring
   ```

3. CRITICAL: This sort MUST happen BEFORE:
   - `computeNodePositions` (line ~199)
   - `this.nodeAnimStates = this.instrumentOrder.map(...)` (line ~202)
   - `this.pairs = buildPairs(this.instrumentOrder)` (line ~207)
   - `edgeAnimStates` creation loop (line ~210)
   All these data structures are index-dependent on `instrumentOrder`. The sort changes which index each instrument occupies, so it must precede all index-dependent construction.

4. Handle edge case: when bass is NOT in lineup, skip the bass-center logic and just sort all instruments by family. The existing code already handles this (`bassIdx > 0` guard) — just ensure the family sort runs on whichever instruments are on the ring.

5. Import `INSTRUMENT_FAMILIES` if not already imported (it may already be imported from Task 1).
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors. With a full 8-instrument lineup, the ring order should be: drums, keyboard, vibes, guitar, saxophone, trumpet, trombone (family-clustered). Bass remains at center.
  </verify>
  <done>
Ring instruments sorted by FAMILY_SORT_ORDER before computeNodePositions/buildPairs/nodeAnimStates. Same-family instruments appear adjacent on the circular layout. Bass stays at index 0 (center). TypeScript compiles clean. Pair keys still resolve correctly (they use instrument names, not indices).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds (Vite production build)
3. Visual check: nodes display colored ring strokes — brass instruments have amber rings, woodwinds have emerald rings, etc.
4. Visual check: same-family instruments are adjacent on the ring (horns cluster, rhythm section clusters)
5. Bass remains at canvas center regardless of family sort
6. Existing Phase 12 confidence indicator (globalAlpha dimming) still works — ring inherits the alpha correctly
</verification>

<success_criteria>
- Every instrument node has a visible family-colored ring outside its role-colored fill
- Ring colors are stable (not animating) and match FAMILY_RING_COLOR constants
- Same-family instruments cluster adjacently on the circular layout
- Bass stays at center, drums on ring adjacent to keyboard family
- No TypeScript errors, no canvas rendering regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-visual-family-identity/13-01-SUMMARY.md`
</output>
