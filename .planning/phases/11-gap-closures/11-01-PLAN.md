---
phase: 11-gap-closures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/InstrumentRoleOverlay.tsx
  - src/components/NodeDetailPanel.tsx
autonomous: true

must_haves:
  truths:
    - "loadExample creates AudioContext synchronously before any await, matching FileUpload pattern"
    - "InstrumentRoleOverlay.tsx no longer exists in the codebase"
    - "No textual references to InstrumentRoleOverlay remain in any source file"
  artifacts:
    - path: "src/App.tsx"
      provides: "iOS-safe loadExample with synchronous AudioContext creation"
      contains: "new AudioContextClass"
    - path: "src/components/NodeDetailPanel.tsx"
      provides: "Updated comments without InstrumentRoleOverlay references"
  key_links:
    - from: "src/App.tsx loadExample"
      to: "audioStateRef.current.audioCtx"
      via: "synchronous assignment before first await"
      pattern: "new AudioContextClass.*sampleRate.*44100"
---

<objective>
Fix iOS Safari AudioContext gesture issue in loadExample and remove dead InstrumentRoleOverlay code.

Purpose: FIX-01 ensures the "Load Example" button works on iOS Safari. FIX-02 removes dead code that misleads future readers.
Output: Patched App.tsx with synchronous AudioContext creation; InstrumentRoleOverlay.tsx deleted; NodeDetailPanel.tsx comments updated.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gap-closures/11-RESEARCH.md

@src/App.tsx
@src/components/FileUpload.tsx
@src/components/InstrumentRoleOverlay.tsx
@src/components/NodeDetailPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS AudioContext gesture fix in loadExample (FIX-01)</name>
  <files>src/App.tsx</files>
  <action>
In the `loadExample` async function (line 120), add a synchronous AudioContext creation block BEFORE the first `await`. This mirrors the proven pattern from `FileUpload.handleButtonClick` (lines 78-103 of FileUpload.tsx).

Add this block at the very top of loadExample's function body, before `const infoRes = await fetch(...)`:

```typescript
// iOS: create AudioContext synchronously within user gesture (before any await).
// Same pattern as FileUpload.handleButtonClick.
const AudioContextClass =
  window.AudioContext ??
  (window as Window & { webkitAudioContext?: typeof AudioContext }).webkitAudioContext;

if (AudioContextClass && !audioStateRef.current.audioCtx) {
  const ctx = new AudioContextClass({ sampleRate: 44100 });
  audioStateRef.current.audioCtx = ctx;
  audioStateRef.current.sampleRate = ctx.sampleRate;
}
```

Do NOT:
- Move the existing `loadAudioBuffer` call or its internal `createAudioContext` fallback
- Add any `await` before this block
- Add an `audioCtx.resume()` here (loadAudioBuffer already handles resume)

The existing code path in `loadAudioBuffer` checks `if (!audioCtx)` before creating one — with the pre-created context, it will skip creation and use the existing one.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no TypeScript errors.
Run `npm run build` to confirm clean production build.
Grep `src/App.tsx` for the pattern: the `new AudioContextClass` call must appear BEFORE any `await` in loadExample.
  </verify>
  <done>loadExample function creates AudioContext synchronously before its first await. TypeScript compiles cleanly. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove InstrumentRoleOverlay dead code (FIX-02)</name>
  <files>src/components/InstrumentRoleOverlay.tsx, src/components/NodeDetailPanel.tsx</files>
  <action>
1. Delete the file `src/components/InstrumentRoleOverlay.tsx` entirely.

2. In `src/components/NodeDetailPanel.tsx`, update the two comments that reference InstrumentRoleOverlay:
   - Line 10: Change "same pattern as InstrumentRoleOverlay" to "same pattern as FileUpload"
   - Line 23: Change "match canvas legend and InstrumentRoleOverlay" to "match canvas legend"

3. Verify no other files reference InstrumentRoleOverlay by running grep across the entire src/ directory. If any import statements are found (unexpected based on research), remove them.
  </action>
  <verify>
Confirm `src/components/InstrumentRoleOverlay.tsx` does not exist: `ls src/components/InstrumentRoleOverlay.tsx` should fail.
Run `grep -r "InstrumentRoleOverlay" src/` — must return zero results.
Run `npx tsc --noEmit` — no TypeScript errors.
Run `npm run build` — clean build.
  </verify>
  <done>InstrumentRoleOverlay.tsx is deleted. Zero references to "InstrumentRoleOverlay" exist in src/. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. In `src/App.tsx`, the `loadExample` function has `new AudioContextClass(...)` before its first `await`
4. `src/components/InstrumentRoleOverlay.tsx` does not exist
5. `grep -r "InstrumentRoleOverlay" src/` returns nothing
</verification>

<success_criteria>
- loadExample creates AudioContext synchronously before any await (FIX-01)
- InstrumentRoleOverlay.tsx deleted, all references removed (FIX-02)
- TypeScript compiles and production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/11-gap-closures/11-01-SUMMARY.md`
</output>
